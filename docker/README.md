## Docker

create docker image

```
pnpm docker:build
```

run docker image

```
pnpm docker:start
```

push docker image to hub

```
docker tag np-services <username>/np-services
docker push <username>/np-services
```
